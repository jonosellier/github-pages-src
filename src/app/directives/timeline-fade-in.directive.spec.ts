import { TimelineFadeInDirective } from './timeline-fade-in.directive';

describe('TimelineFadeInDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineFadeInDirective();
    expect(directive).toBeTruthy();
  });
});
